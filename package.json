{
	"name": "time-fns",
  "version": "1.0.0",
  "description": "Functions for working with units of time.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "lint": "tslint --format stylish --project .",
    "test": "jest",
    "prepublish": "npm run test && npm run lint && npm run build"
  },
  "author": "Daniel Levett <dlevett@hotmail.co.uk>",
  "repository": {
    "type": "git",
    "url": "https://github.com/dlevs/time-fns"
  },
  "license": "MIT",
  "dependencies": {
    "@dlevs/tslint-config": "^1.0.3"
  },
  "devDependencies": {
    "@types/deep-freeze": "^0.1.1",
    "@types/jest": "^23.1.5",
    "deep-freeze": "0.0.1",
    "jest": "^23.3.0",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.0.0",
    "tslint": "^5.10.0",
    "tslint-xo": "^0.8.0",
    "typescript": "^3.1.3"
  }
}
